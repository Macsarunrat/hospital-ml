# ตัวอย่างไฟล์คอนฟิก (คัดลอกไฟล์นี้แล้วเปลี่ยนชื่อเป็น .env)
# 1. รหัส Token สำหรับอ่าน GitHub Private Repo (ถ้า Repo เป็น Public ไม่ต้องใส่)
GITHUB_TOKEN=""

# 2. รหัส API Key สำหรับดูกราฟบน WandB Dashboard
WANDB_API_KEY=""
